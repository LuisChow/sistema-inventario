/**
 * Este archivo se mantiene vacío intencionalmente.
 *
 * La aplicación NO usa Angular Router. Las vistas internas (inicio, inventario,
 * historial, configuración) se controlan mediante `inventoryService.vistaActual`
 * y se renderizan condicionalmente en `main-view.html`. Ver `app.config.ts`
 * para más detalles sobre por qué.
 *
 * Si más adelante migramos a una arquitectura con rutas reales (por ejemplo
 * al pasar a SaaS web con deep-links), aquí van a vivir las definiciones.
 */
